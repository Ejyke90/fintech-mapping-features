# Gradle properties for CI/CD and default builds
# NOTE: For local environment overrides, create a 'gradle-local.properties' file
# Example: org.gradle.java.home=/path/to/your/jdk

# Gradle daemon performance settings
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true